export const NotificationService = {
    schedule: async () => { console.warn("NotificationService.schedule is not fully implemented in Desktop mode yet"); },
    cancel: async () => { console.warn("NotificationService.cancel is not fully implemented in Desktop mode yet"); },
    requestPermissions: async () => { console.warn("NotificationService.requestPermissions is not fully implemented in Desktop mode yet"); }
};
