---
phase: 01-project-scaffolding-authentication
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/features/auth/components/LoginForm.tsx
  - src/features/auth/components/RegisterForm.tsx
  - src/features/auth/components/ResetPasswordForm.tsx
  - api/middleware/auth.ts
  - api/routes/auth.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
---

<objective>
Implement full user authentication flows using the new Supabase-managed authentication system.

Purpose: Provide user signup, login, logout, and password recovery functionality.
Output: Refactored auth UI and backend verification middleware.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-authentication/01-01-SUMMARY.md
@.planning/phases/01-project-scaffolding-authentication/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: User Sign Up & In UI Implementation</name>
  <files>
    - src/features/auth/components/LoginForm.tsx
    - src/features/auth/components/RegisterForm.tsx
  </files>
  <action>
    - Refactor existing forms to use `supabase.auth.signInWithPassword` and `supabase.auth.signUp`.
    - Handle loading states with Shadcn Skeletons or button spinners.
    - Implement error handling with toast notifications (sonner or similar).
    - Ensure successful signup/login redirects to `/onboarding` or `/dashboard`.
  </action>
  <verify>
    - Submit login with test credentials; successful sign-in should trigger redirect.
    - Submit register with a new email; check Supabase dashboard for the new user.
  </verify>
  <done>User can sign up and log in via the web UI using Supabase Auth.</done>
</task>

<task type="auto">
  <name>Task 2: Logout & Password Reset Flow</name>
  <files>
    - src/features/auth/components/ResetPasswordForm.tsx
  </files>
  <action>
    - Add a "Logout" action to the UI calling `supabase.auth.signOut`.
    - Implement a "Forgot Password" request form that calls `supabase.auth.resetPasswordForEmail`.
    - Create a "Reset Password" page that allows users to input a new password after clicking an email link.
    - Ensure the reset URL is handled by the frontend and redirects to the reset page.
  </action>
  <verify>
    - Clicking logout clears session and redirects to /login.
    - Requesting a password reset sends an email; clicking the link shows the reset page.
  </verify>
  <done>User can securely log out and recover forgotten passwords.</done>
</task>

<task type="auto">
  <name>Task 3: Backend Auth Middleware Migration</name>
  <files>
    - api/middleware/auth.ts
    - api/routes/auth.ts
  </files>
  <action>
    - Update `api/middleware/auth.ts` to use `supabase.auth.getUser(token)` for session validation.
    - Ensure the Express backend correctly handles the Supabase JWT from the Authorization header or cookies.
    - Refactor `api/routes/auth.ts` to remove hand-rolled registration/login logic and instead provide a `/sync` endpoint if needed for extra profile data.
    - Ensure all existing backend routes continue to work with the new auth middleware.
  </action>
  <verify>
    - Sending a request to a protected API route with a valid Supabase JWT returns 200.
    - Sending an invalid/expired token returns 401.
  </verify>
  <done>Backend services are secured using Supabase authentication tokens.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "User can register a new account with email/password"
    - "User can log in with valid credentials"
    - "User can log out and clear the session"
    - "User can reset a forgotten password"
    - "Backend routes are protected by the Supabase JWT"
  artifacts:
    - path: "src/features/auth/components/LoginForm.tsx"
      provides: "Login UI"
    - path: "api/middleware/auth.ts"
      provides: "JWT Verification"
  key_links:
    - from: "LoginForm.tsx"
      to: "supabase.auth.signInWithPassword"
      via: "onSubmit handler"
    - from: "api/middleware/auth.ts"
      to: "supabase.auth.getUser"
      via: "authenticateToken middleware"
</must_haves>

<success_criteria>
1. All core authentication flows (Login, Register, Logout, Reset) are functional.
2. The Express backend correctly identifies the authenticated user via Supabase.
3. User experience is smooth with proper loading/error states.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-authentication/01-02-SUMMARY.md`
</output>
