---
phase: 01-project-scaffolding-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
  - src/shared/lib/supabase.ts
  - src/features/auth/contexts/AuthContext.tsx
  - src/shared/stores/authStore.ts
  - src/app/App.tsx
autonomous: true
requirements: [AUTH-02]
---

<objective>
Establish the technical foundation for Supabase-managed authentication and global auth state.

Purpose: Shift from hand-rolled JWTs to Supabase Auth for better security and persistence.
Output: Initialized Supabase client, AuthProvider, and Zustand auth store.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-authentication/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Supabase Client & Environment</name>
  <files>
    - .env
    - src/shared/lib/supabase.ts
  </files>
  <action>
    - Ensure VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are in .env.
    - Create/update src/shared/lib/supabase.ts to export a singleton createClient instance.
    - Export a helper function to get the current session.
  </action>
  <verify>
    - `npm run lint` passes
    - Console log of `supabase` object in a temporary component shows initialized state.
  </verify>
  <done>Supabase client is configured and accessible throughout the frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AuthProvider & Zustand Auth Store</name>
  <files>
    - src/shared/stores/authStore.ts
    - src/features/auth/contexts/AuthContext.tsx
  </files>
  <action>
    - Create `useAuthStore` in Zustand to manage { user, session, isLoading, error }.
    - Implement `AuthContext` using `supabase.auth.onAuthStateChange` to sync Supabase session with the Zustand store.
    - Ensure the provider wraps the root application in App.tsx.
    - Handle initial session loading state to prevent UI flickers.
  </action>
  <verify>
    - Zustand devtools shows auth state syncing on refresh.
    - Provider renders children successfully.
  </verify>
  <done>Global auth state is managed by Supabase and reactive to session changes.</done>
</task>

<task type="auto">
  <name>Task 3: Routing & Protected Routes</name>
  <files>
    - src/app/App.tsx
  </files>
  <action>
    - Create `ProtectedRoute` and `PublicOnlyRoute` components (or similar logic in the router).
    - Protected routes should redirect to /login if unauthenticated.
    - PublicOnly routes (like /login, /register) should redirect to /dashboard if already authenticated.
    - Wire up basic route stubs for /, /login, /register, /dashboard, and /onboarding.
  </action>
  <verify>
    - Navigating to /dashboard while logged out redirects to /login.
    - Navigating to /login while logged in redirects to /dashboard.
  </verify>
  <done>Routing logic correctly enforces authentication boundaries.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "Supabase client is initialized with environment variables"
    - "Application state reflects the current Supabase session"
    - "Auth state persists across page refreshes"
  artifacts:
    - path: "src/shared/lib/supabase.ts"
      provides: "Supabase client singleton"
    - path: "src/features/auth/contexts/AuthContext.tsx"
      provides: "Session listener and provider"
  key_links:
    - from: "src/features/auth/contexts/AuthContext.tsx"
      to: "supabase.auth.onAuthStateChange"
      via: "useEffect hook"
</must_haves>

<success_criteria>
1. Supabase client initialized and connected.
2. Global auth state correctly identifies logged-in/logged-out status.
3. Protected routes prevent unauthorized access.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-authentication/01-01-SUMMARY.md`
</output>
