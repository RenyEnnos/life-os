---
phase: 01-project-scaffolding-authentication
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/features/onboarding/components/OnboardingFlow.tsx
  - src/shared/stores/onboardingStore.ts
  - api/routes/onboarding.ts
  - supabase/migrations/20260226_create_profiles.sql
autonomous: true
requirements: [AUTH-01]
---

<objective>
Implement the multi-step onboarding flow for new users to initialize their profiles and preferences.

Purpose: Collect initial user data before allowing access to the full dashboard.
Output: A functional 5-step onboarding UI and a Supabase profiles table.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-authentication/01-02-SUMMARY.md
@.planning/phases/01-project-scaffolding-authentication/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding State Store & UI</name>
  <files>
    - src/shared/stores/onboardingStore.ts
    - src/features/onboarding/components/OnboardingFlow.tsx
  </files>
  <action>
    - Create `useOnboardingStore` in Zustand with `persist` middleware to track current step and form data.
    - Implement a 5-step onboarding wizard:
      1. Profile basic details (name, nickname).
      2. Life goals/priorities.
      3. Focus areas (Habits vs. Finance vs. Tasks).
      4. UI/Theme preferences.
      5. Final summary and confirmation.
    - Use Framer Motion for step transitions and Shadcn UI for form elements.
  </action>
  <verify>
    - User can navigate between steps using "Back" and "Next" buttons.
    - Refreshing the page during onboarding retains the current step and data.
  </verify>
  <done>Onboarding UI is functional and preserves state across refreshes.</done>
</task>

<task type="auto">
  <name>Task 2: Database Schema & Profiles Initialization</name>
  <files>
    - supabase/migrations/20260226_create_profiles.sql
  </files>
  <action>
    - Create a `profiles` table in Supabase that links to `auth.users.id`.
    - Set up a Supabase database trigger to automatically insert a new profile row when a user signs up.
    - Configure Row-Level Security (RLS) policies so users can only view and edit their own profiles.
  </action>
  <verify>
    - New signups in Supabase Auth automatically create a corresponding row in the `profiles` table.
    - Supabase dashboard shows RLS is active for the table.
  </verify>
  <done>Profiles database schema is established and automatically initialized for new users.</done>
</task>

<task type="auto">
  <name>Task 3: Onboarding Completion & Redirect</name>
  <files>
    - src/features/onboarding/components/OnboardingFlow.tsx
    - api/routes/onboarding.ts
  </files>
  <action>
    - Finalize onboarding by submitting all collected data to the backend or Supabase `profiles` table.
    - Update the user profile to mark onboarding as completed (`onboarding_completed: true`).
    - Redirect to the Dashboard upon successful completion.
    - Ensure unauthenticated users or users who have already completed onboarding are redirected away from the onboarding page.
  </action>
  <verify>
    - After the 5th step, submitting redirects to /dashboard.
    - After completion, navigating back to /onboarding redirects to /dashboard.
  </verify>
  <done>User onboarding correctly updates the user profile and unlocks the dashboard.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "New users must complete onboarding before reaching the dashboard"
    - "Onboarding progress is saved across page refreshes"
    - "Profile data is stored in a secure table linked to the user's ID"
  artifacts:
    - path: "src/features/onboarding/components/OnboardingFlow.tsx"
      provides: "Multi-step UI"
    - path: "supabase/migrations/20260226_create_profiles.sql"
      provides: "Database schema and RLS"
  key_links:
    - from: "OnboardingFlow.tsx"
      to: "profiles"
      via: "PATCH /api/auth/profile or direct Supabase update"
</must_haves>

<success_criteria>
1. A multi-step onboarding flow successfully captures user preferences.
2. User profile data is persistent and securely stored in Supabase.
3. Onboarding serves as a gateway to the main application for new users.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-authentication/01-03-SUMMARY.md`
</output>
