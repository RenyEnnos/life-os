# Plan: 09-02 AI Resonance Integration

**Phase:** 09 - Journaling & AI Reflection
**Plan:** 09-02
**Status:** Ready to execute

## Goal
Integrate the AI "Neural Resonance" analysis so that every journal entry is automatically analyzed for mood, themes, and insights.

## Context
- The `triggerJournalAnalysis` API is already defined.
- We need to trigger this analysis whenever a user finishes writing or clicks a "Scan" button.

## Task Breakdown
1. **Auto-Trigger Logic**: Add logic to `JournalEditor` to trigger `triggerJournalAnalysis(id)` after a successful save.
   - Files: `src/features/journal/components/JournalEditor.tsx`
2. **Real-time Insights**: Update `ResonancePanel` to display the `JournalInsight` data associated with the current entry.
   - Files: `src/features/journal/components/resonance/ResonancePanel.tsx`
3. **Loading States**: Add a premium "AI Scanning" animation while the analysis is in progress.

## Verification
- [ ] Saving an entry triggers a backend call to `/api/resonance/analyze`.
- [ ] Insights (Mood score, themes) appear in the side panel after analysis.
- [ ] Error handling handles AI timeouts gracefully.

## Agent Assignments
- **Integration**: `backend-specialist`
- **UI Feedback**: `frontend-specialist`, `frontend-design`
