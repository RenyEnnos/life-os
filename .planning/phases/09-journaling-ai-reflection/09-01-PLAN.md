# Plan: 09-01 Journal Refactoring & CRUD

**Phase:** 09 - Journaling & AI Reflection
**Plan:** 09-01
**Status:** Ready to execute

## Goal
Replace the mock-heavy `JournalPage.tsx` with a fully functional, data-driven implementation that supports creating, viewing, and deleting entries.

## Context
- `JournalPage.tsx` currently contains hardcoded UI and SVG.
- Features like `JournalEntryList`, `JournalEditor`, and `JournalSidebar` already exist but are not properly integrated.

## Task Breakdown
1. **Hook Integration**: Use the `useJournal` hook in `JournalPage.tsx` to manage entries and selected state.
   - Files: `src/features/journal/pages/JournalPage.tsx`
2. **Component Replacement**:
   - Replace the static sidebar with `JournalEntryList`.
   - Replace the main area with `JournalEditor`.
   - Replace the right insights panel with `ResonancePanel`.
3. **Draft Support**: Implement basic local state for new entries (Drafts) before they are saved to Supabase.

## Verification
- [ ] List of entries accurately reflects the database.
- [ ] Selecting an entry in the sidebar loads it into the editor.
- [ ] Deleting an entry updates the UI immediately.

## Agent Assignments
- **Frontend**: `frontend-specialist`, `frontend-design`
- **Data**: `backend-specialist`
