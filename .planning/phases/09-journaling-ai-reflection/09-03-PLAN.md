# Plan: 09-03 Mood Trends & Visualization

**Phase:** 09 - Journaling & AI Reflection
**Plan:** 09-03
**Status:** Ready to execute

## Goal
Implement a real mood trend visualization using Recharts to replace the mock CSS bars.

## Context
- The user needs to see their emotional progress over time.
- `useJournalInsights` already provides a `moodTrend` array.

## Task Breakdown
1. **MoodTrendChart Component**: Create a line/area chart using `recharts` that displays mood scores over the last 7-30 days.
   - Files: `src/features/journal/components/MoodTrendChart.tsx`
2. **Dashboard Integration**: Replace the mock chart in `JournalPage` or the main dashboard with this real component.
3. **Sentiment Colors**: Map mood scores (1-10) to the Life OS primary/success/warning color palette.

## Verification
- [ ] Chart accurately plots the mood scores from journal insights.
- [ ] X-axis shows the correct dates or week days.
- [ ] Tooltip shows the associated entry summary or mood label.

## Agent Assignments
- **Data Viz**: `frontend-specialist`, `frontend-design`
