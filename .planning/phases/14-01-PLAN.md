---
phase: 14-hybrid-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240401000000_add_position_to_tasks.sql
  - package.json
  - api/services/tasksService.ts
  - src/features/tasks/hooks/useTasks.ts
  - src/features/tasks/index.tsx
  - src/features/tasks/components/TaskItem.tsx
autonomous: true
requirements: [GEMS-03]
---

<objective>
Implement hybrid task interactivity by adding support for manual task reordering in the List View using dnd-kit and fractional indexing for persistence.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/features/tasks/hooks/useTasks.ts
@src/features/tasks/index.tsx
@api/services/tasksService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure and Database Setup</name>
  <files>
    - supabase/migrations/20240401000000_add_position_to_tasks.sql
    - package.json
    - api/services/tasksService.ts
  </files>
  <action>
    1. Create a Supabase migration to add a `position` (TEXT) column to the `tasks` table. Initialize it with a default value (e.g., based on created_at) for existing records.
    2. Install `fractional-indexing` library using `npm install fractional-indexing`.
    3. Update `api/services/tasksService.ts` to include the `position` field in CRUD operations and ensure the `getAll` query sorts by `position ASC` by default.
  </action>
  <verify>
    - `npm list fractional-indexing` shows the library installed.
    - SQL migration file exists and is correctly formatted.
    - Backend API returns tasks ordered by the new position column.
  </verify>
  <done>Database supports positioning and library is available for use.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sortable List UI and Hook Update</name>
  <files>
    - src/features/tasks/hooks/useTasks.ts
    - src/features/tasks/index.tsx
    - src/features/tasks/components/TaskItem.tsx
  </files>
  <action>
    1. Update `useTasks` hook:
       - Ensure `tasks` are sorted by `position` in the frontend.
       - Verify `updateTask` mutation correctly handles the `position` field.
    2. Refactor `TasksPage.tsx` (src/features/tasks/index.tsx):
       - Wrap the list container with `DndContext`.
       - Wrap task items with `SortableContext` using the `verticalListSortingStrategy`.
       - Implement `handleDragEnd` function to calculate the new position using `generateKeyBetween` from `fractional-indexing`.
    3. Update `TaskItem.tsx` to use the `useSortable` hook.
  </action>
  <verify>
    - Dragging a task in the list updates its position visually.
    - Refreshing the page maintains the new order.
  </verify>
  <done>List view supports persistent drag-and-drop reordering.</done>
</task>

</tasks>

<success_criteria>
- User can toggle to List view and reorder tasks.
- Reordering uses fractional indexing.
- UI remains responsive with optimistic updates.
</success_criteria>
