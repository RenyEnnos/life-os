# Plan: 05-01 Finances Page Refactoring

**Phase:** 05 - Financial Management
**Plan:** 05-01
**Status:** Ready to execute

## Goal
Refactor `FinancesPage.tsx` to replace the existing mock HTML and SVG with the interactive components (`FinanceCharts`, `FinanceSummaryCards`, `TransactionForm`).

## Context
- `FinancesPage.tsx` is currently a placeholder with static data.
- Sub-components for real charts and forms are already available but not integrated into the main page.

## Task Breakdown
1. **Hook Integration**: Import and use `useTransactions` and `useFinances` in `FinancesPage.tsx`.
   - Files: `src/features/finances/pages/FinancesPage.tsx`
2. **Component Replacement**:
   - Replace the mock "Stats Summary Cards" with `FinanceSummaryCards`.
   - Replace the mock SVG chart with `FinanceCharts`.
   - Implement the `Add Transaction` button to trigger the `TransactionModal`.
3. **Data Wiring**: Pass the fetched `transactions` and `summary` data into the charts and summary components.
   - Files: `src/features/finances/pages/FinancesPage.tsx`

## Verification
- [ ] Finances page displays real data from the database.
- [ ] Summary cards correctly show the total balance, income, and expenses.
- [ ] The "Add Transaction" button correctly opens the transaction form.

## Agent Assignments
- **Frontend**: `frontend-specialist`
- **Data Logic**: `backend-specialist`
