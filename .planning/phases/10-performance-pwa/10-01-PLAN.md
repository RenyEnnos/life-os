# Plan: 10-01 PWA Infrastructure

**Phase:** 10 - Performance Optimization & PWA
**Plan:** 10-01
**Status:** Ready to execute

## Goal
Ensure the application meets all PWA installation requirements by fixing icon paths and manifest configuration.

## Context
- The current manifest points to missing `.png` files.
- `public/` folder contains high-quality `.svg` icons.

## Task Breakdown
1. **Manifest Update**: Update `vite.config.ts` to use `.svg` icons or add appropriate `.png` versions if required for compatibility.
2. **Apple Touch Icon**: Ensure the `apple-touch-icon.png` is correctly linked and present in `public/`.
3. **PWA Colors**: Set appropriate `theme_color` and `background_color` in the manifest to match the Life OS dark theme.

## Verification
- [ ] Browser's DevTools -> Application -> Manifest shows no errors.
- [ ] "Install App" button appears in compatible browsers.
- [ ] Theme colors match the OLED background of the app.

## Agent Assignments
- **PWA Config**: `devops-engineer`
- **Asset Creation**: `frontend-design`
