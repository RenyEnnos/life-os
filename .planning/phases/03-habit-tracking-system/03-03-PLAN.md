---
phase: 3
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [src/features/habits/components/HabitHeatmap.tsx, src/features/habits/logic/streak.ts, src/features/habits/components/HabitAnalytics.tsx]
autonomous: true
requirements: [HABIT-03, HABIT-04]

must_haves:
  truths:
    - "Heatmap accurately reflects 90 days of habit consistency"
    - "Streaks and completion rates correctly handle today/yesterday completion logic"
  artifacts:
    - path: "src/features/habits/logic/streak.ts"
      provides: "Refined streak and completion rate calculation logic"
  key_links:
    - from: "src/features/habits/components/HabitHeatmap.tsx"
      to: "src/features/habits/api/habits.api.ts"
      via: "fetchHabitLogs query"
    - from: "src/features/habits/components/HabitAnalytics.tsx"
      to: "src/features/habits/logic/streak.ts"
      via: "calculateCompletionRate function"
---

<objective>
Implement advanced analytics, including heatmaps, verified streak logic, and completion rate calculations.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Wire Heatmap to Real Data</name>
  <files>src/features/habits/components/HabitHeatmap.tsx</files>
  <action>
    - Update `HabitHeatmap.tsx` to use `useHabitLogs` to fetch actual habit log data from the last 90 days.
    - Map completion levels to heatmap intensities based on percentage of habits completed per day.
  </action>
  <verify>
    Verify heatmap intensities match log counts for specific days in the data provider.
    <automated>vitest src/features/habits/components/__tests__/HabitHeatmap.test.tsx</automated>
  </verify>
  <done>Visual consistency tracking is data-driven and accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Streak and Completion Rate Logic</name>
  <files>src/features/habits/logic/streak.ts, src/features/habits/components/HabitAnalytics.tsx</files>
  <action>
    - Review and update streak logic in `streak.ts` to properly handle "Today" vs "Yesterday".
    - Implement `calculateCompletionRate` function to determine percentage of successful days over a given period.
    - Create `HabitAnalytics.tsx` component to display these metrics (Current Streak, Best Streak, Completion %).
  </action>
  <verify>
    Run unit tests for streak logic and completion rate calculations.
    <automated>vitest src/features/habits/logic/__tests__/streak.test.ts</automated>
  </verify>
  <done>Accurate streak and analytics tracking for all habits.</done>
</task>
</tasks>
