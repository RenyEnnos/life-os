---
phase: 3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/features/habits/api/habits.api.ts, src/shared/types.ts, src/features/habits/components/CreateHabitDialog.tsx, src/features/habits/components/HabitCard.tsx]
autonomous: true
requirements: [HABIT-01, HABIT-05]

must_haves:
  truths:
    - "Users can create, edit, and delete both binary and quantified (goal-based) habits"
    - "Habit definitions are persisted to Supabase with support for colors and icons"
  artifacts:
    - path: "src/features/habits/api/habits.api.ts"
      provides: "Full CRUD methods supporting quantified goals, colors, and icons"
  key_links:
    - from: "src/features/habits/components/CreateHabitDialog.tsx"
      to: "src/features/habits/api/habits.api.ts"
      via: "form submission (create/update)"
    - from: "src/features/habits/components/HabitCard.tsx"
      to: "src/features/habits/api/habits.api.ts"
      via: "deleteHabit mutation"
---

<objective>
Refine core habit management to support quantified habits and full CRUD operations, including discretionary visual features.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update API and Types for Quantified Habits, Colors, and Icons</name>
  <files>src/shared/types.ts, src/features/habits/api/habits.api.ts</files>
  <action>
    - Update `Habit` type to include `target_value`, `unit`, `type` (binary/quantified), `color`, and `icon`.
    - Update `habitsApi.create`, `update`, and `delete` to handle these fields and wire to Supabase.
  </action>
  <verify>
    Check types and API methods.
    <automated>vitest src/features/habits/api/__tests__/habits.api.test.ts</automated>
  </verify>
  <done>Backend interaction layer supports all v1 requirements and discretionary features.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Create/Edit/Delete UI</name>
  <files>src/features/habits/components/CreateHabitDialog.tsx, src/features/habits/components/HabitCard.tsx</files>
  <action>
    - Update `CreateHabitDialog.tsx` to include fields for habit type, target value, unit, color selection, and icon selection.
    - Wire the dialog to `habitsApi.create` and `habitsApi.update`.
    - Add a delete button to `HabitCard.tsx` and wire it to `habitsApi.delete`.
  </action>
  <verify>
    Open dialog and confirm all fields appear. Trigger a deletion and confirm the habit is removed.
    <automated>vitest src/features/habits/components/__tests__/CreateHabitDialog.test.tsx</automated>
  </verify>
  <done>User can manage habits with full feature support via UI.</done>
</task>
</tasks>
