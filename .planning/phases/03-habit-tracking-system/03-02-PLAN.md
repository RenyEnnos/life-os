---
phase: 3
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/features/habits/hooks/useHabits.ts, src/features/dashboard/widgets/HabitWidget.tsx]
autonomous: true
requirements: [HABIT-02]

must_haves:
  truths:
    - "Habit logging provides instant visual feedback via optimistic updates"
    - "Errors during logging are handled with state rollback"
  artifacts:
    - path: "src/features/habits/hooks/useHabits.ts"
      provides: "Optimistic logging mutation with cache invalidation"
  key_links:
    - from: "src/features/dashboard/widgets/HabitWidget.tsx"
      to: "src/features/habits/hooks/useHabits.ts"
      via: "toggleHabit mutation"
---

<objective>
Implement optimistic UI updates for habit logging and integrate with the dashboard widget.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement Optimistic Updates in useHabits Hook</name>
  <files>src/features/habits/hooks/useHabits.ts</files>
  <action>
    - Add `onMutate`, `onError`, and `onSettled` handlers to the habit logging mutation.
    - Snapshot current habits state before mutation.
    - Update local cache immediately when a habit is logged (binary toggle or quantified increment).
    - Roll back to snapshot if the API call fails.
  </action>
  <verify>
    Log a habit and confirm immediate state change before network completion.
    <automated>vitest src/features/habits/hooks/__tests__/useHabits.test.tsx</automated>
  </verify>
  <done>Zero-latency feel for habit logging.</done>
</task>

<task type="auto">
  <name>Task 2: Update HabitWidget for Quantified Logging</name>
  <files>src/features/dashboard/widgets/HabitWidget.tsx</files>
  <action>
    - Update the `HabitWidget` to detect quantified habits and use the `toggleHabit` mutation from `useHabits`.
    - Add increment/decrement buttons or a value input for quantified habits.
    - Ensure logging works correctly for both binary and quantified types.
  </action>
  <verify>
    Verify that clicking log buttons triggers UI update instantly.
    <automated>vitest src/features/dashboard/widgets/__tests__/HabitWidget.test.tsx</automated>
  </verify>
  <done>Dashboard widget supports all habit interactions with zero latency.</done>
</task>
</tasks>
