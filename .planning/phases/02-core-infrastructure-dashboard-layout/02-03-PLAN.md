---
phase: 02-core-infrastructure-dashboard-layout
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/shared/ui/Widget.tsx, src/features/dashboard/widgets/HabitWidget.tsx, src/features/dashboard/widgets/TaskWidget.tsx]
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "Widgets show loading skeletons during data fetching"
    - "Widgets show illustrative empty states when no data is present"
  artifacts:
    - path: "src/shared/ui/Widget.tsx"
      provides: "Skeleton and Empty State sub-components"
  key_links:
    - from: "src/features/dashboard/widgets/HabitWidget.tsx"
      to: "src/shared/ui/Widget.tsx"
      via: "isLoading/isEmpty props"
---

<objective>
Finalize the widget framework by implementing loading and empty states, and ensuring visual polish across the dashboard.

Purpose: Provide a polished user experience that handles data lifecycle states gracefully.
Output: Widgets with skeletons and empty states.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/shared/ui/Widget.tsx
@src/features/dashboard/widgets/HabitWidget.tsx
@src/features/dashboard/widgets/TaskWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Widget Skeleton & Empty States</name>
  <files>src/shared/ui/Widget.tsx</files>
  <action>
    - Add `WidgetSkeleton` and `WidgetEmptyState` sub-components to `src/shared/ui/Widget.tsx`.
    - `WidgetSkeleton`: A pulsant skeleton matching the typical widget content area.
    - `WidgetEmptyState`: A centered state with an optional icon and message.
    - Update the `Widget` component to automatically render these states when `isLoading` or `isEmpty` is true.
  </action>
  <verify>
    Check `src/shared/ui/Widget.tsx` for the new sub-components.
  </verify>
  <done>
    Widget component supports all lifecycle states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Habit & Task Widgets</name>
  <files>src/features/dashboard/widgets/HabitWidget.tsx, src/features/dashboard/widgets/TaskWidget.tsx</files>
  <action>
    - Pass `isLoading` and `isEmpty` props from `HabitWidget` and `TaskWidget` to the `Widget` wrapper.
    - Ensure `isEmpty` is calculated correctly (e.g., `habits.length === 0`).
    - Remove old manual loading/empty logic from these widgets to use the centralized framework.
  </action>
  <verify>
    Verify that `HabitWidget` and `TaskWidget` pass the state props.
  </verify>
  <done>
    Core widgets use the centralized state framework.
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Polished widget lifecycle states</what-built>
  <how-to-verify>
    - Simulate loading state (e.g., by temporarily setting `isLoading={true}` in `Widget.tsx`).
    - Confirm skeleton appears and matches dimensions.
    - Clear tasks/habits (or use a fresh user account) to confirm empty states appear correctly.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure widgets show skeletons when fetching data.
Ensure widgets show "No habits/tasks yet" when empty.
</verification>

<success_criteria>
- Centralized `WidgetSkeleton` and `WidgetEmptyState` implemented.
- `HabitWidget` and `TaskWidget` correctly display these states.
- Dashboard feels responsive and stable during data loads.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-infrastructure-dashboard-layout/02-03-SUMMARY.md`
</output>
