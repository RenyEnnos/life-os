---
phase: 02-core-infrastructure-dashboard-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/shared/ui/BentoCard.tsx, src/shared/ui/BentoGrid.tsx, src/shared/ui/Widget.tsx, src/features/dashboard/components/HabitWidget.tsx, src/features/dashboard/components/TaskWidget.tsx, src/features/dashboard/components/NowWidget.tsx, src/features/dashboard/components/HealthWidget.tsx]
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "A unified Widget abstraction exists that wraps BentoCard"
    - "Widgets handle loading (skeleton) and empty (EmptyState) scenarios consistently"
    - "Placeholder widgets for Habits, Tasks, Health, and Now zones are available"
  artifacts:
    - path: "src/shared/ui/Widget.tsx"
      provides: "Universal widget wrapper for bento boxes"
    - path: "src/shared/ui/BentoGrid.tsx"
      provides: "Reusable grid container (extracted from BentoCard.tsx)"
  key_links:
    - from: "src/shared/ui/Widget.tsx"
      to: "src/shared/ui/BentoCard.tsx"
      via: "Composition"
---

<objective>
Establish the foundational widget framework and feature components needed for the dashboard layout. This includes extracting the grid container and creating a robust, state-aware Widget abstraction.

Purpose: Decouple layout orchestration from widget implementation and state handling.
Output: Widget framework and feature-specific placeholders (Habit, Task, Now, Health).
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
@/home/pedro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-infrastructure-dashboard-layout/02-CONTEXT.md
@.planning/phases/02-core-infrastructure-dashboard-layout/02-RESEARCH.md
@src/shared/ui/BentoCard.tsx
@src/shared/ui/EmptyState.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure Cleanup & Widget Abstraction</name>
  <files>src/shared/ui/BentoGrid.tsx, src/shared/ui/BentoCard.tsx, src/shared/ui/Widget.tsx</files>
  <action>
    1. Extract the `BentoGrid` component from `src/shared/ui/BentoCard.tsx` into its own file `src/shared/ui/BentoGrid.tsx`. Update imports in `BentoCard.tsx` if necessary.
    2. Create `src/shared/ui/Widget.tsx`. This component should:
       - Wrap `BentoCard`.
       - Accept props: `title`, `icon`, `isLoading`, `isEmpty`, `emptyMessage`, `children`, `action` (header slot), `footer` (footer slot).
       - Implement an internal `Skeleton` (using Tailwind `animate-pulse`) when `isLoading` is true.
       - Use the `EmptyState` component (from `src/shared/ui/EmptyState.tsx`) when `isEmpty` is true.
       - Maintain the bento aesthetics (spotlight, border, backdrop blur).
  </action>
  <verify>
    npm run build (to check for import/type errors) and verify files exist.
  </verify>
  <done>Infrastructure is refactored and Widget framework is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard Feature Widgets</name>
  <files>src/features/dashboard/components/HabitWidget.tsx, src/features/dashboard/components/TaskWidget.tsx, src/features/dashboard/components/NowWidget.tsx, src/features/dashboard/components/HealthWidget.tsx</files>
  <action>
    Create placeholder components for the dashboard zones using the `Widget` framework.
    - HabitWidget: Mock header "Habits", icon "Activity", shows EmptyState for now.
    - TaskWidget: Mock header "Tasks", icon "CheckCircle", shows EmptyState for now.
    - NowWidget: Mock header "Now", icon "Clock", shows a simple "No urgent items" message.
    - HealthWidget: Mock header "Health", icon "Heart", shows mock metrics (Sleep, Steps).
    
    Use Lucide icons (Activity, CheckCircle, Clock, Heart) consistently.
  </action>
  <verify>
    Check if components export correctly and render with basic mock content.
  </verify>
  <done>Four specific widgets are created and ready for orchestration.</done>
</task>

</tasks>

<verification>
Verify build passes and components are modular.
</verification>

<success_criteria>
- BentoGrid is a separate file.
- Widget.tsx correctly handles isLoading/isEmpty states.
- Feature widgets are implemented as thin wrappers around Widget.tsx.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-infrastructure-dashboard-layout/02-01-SUMMARY.md`
</output>
