---
phase: 02-core-infrastructure-dashboard-layout
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/features/dashboard/pages/IndexPage.tsx, src/shared/ui/BentoGrid.tsx]
autonomous: false
requirements: [DASH-01]

must_haves:
  truths:
    - "User can clearly distinguish three zones: Now, Today, and Context"
    - "The layout is responsive (1 col on mobile, 3 cols on desktop)"
    - "Widgets animate into view on page load"
  artifacts:
    - path: "src/features/dashboard/pages/IndexPage.tsx"
      provides: "Dashboard entry point with 3-zone grid orchestration"
  key_links:
    - from: "src/features/dashboard/pages/IndexPage.tsx"
      to: "src/shared/ui/BentoGrid.tsx"
      via: "Container component"
    - from: "src/features/dashboard/pages/IndexPage.tsx"
      to: "src/features/dashboard/components/*.tsx"
      via: "Widget placement"
---

<objective>
Orchestrate the feature widgets into the final 3-zone dashboard layout, ensuring responsiveness, interactivity, and fluid animations.

Purpose: Provide the primary user experience for the Life OS dashboard.
Output: Animated, responsive 3-zone dashboard.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
@/home/pedro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-infrastructure-dashboard-layout/02-CONTEXT.md
@.planning/phases/02-core-infrastructure-dashboard-layout/02-01-SUMMARY.md
@src/features/dashboard/pages/IndexPage.tsx
@src/shared/ui/BentoGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrate 3-Zone Layout Grid</name>
  <files>src/features/dashboard/pages/IndexPage.tsx</files>
  <action>
    Refactor IndexPage.tsx to implement the 3-zone layout as defined in the context:
    1. Define the zones using CSS Grid (via Tailwind):
       - Zone 1 (Now): Positioned for immediate attention (top left).
       - Zone 2 (Today): Center focus for Habits and Tasks.
       - Zone 3 (Context): Insights and Health metrics (side or bottom).
    2. Place the widgets created in Plan 01 into their respective zones:
       - NowWidget -> Now Zone.
       - HabitWidget, TaskWidget -> Today Zone.
       - HealthWidget -> Context Zone.
    3. Replace the static mockup code in IndexPage.tsx with these dynamic components.
  </action>
  <verify>
    npm run build passes and file contains references to all 4 feature widgets.
  </verify>
  <done>IndexPage orchestrates the dashboard widgets into three clear zones.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive Design & Transitions</name>
  <files>src/features/dashboard/pages/IndexPage.tsx, src/shared/ui/BentoGrid.tsx</files>
  <action>
    1. Refine `src/shared/ui/BentoGrid.tsx` and its usage in `IndexPage.tsx` to handle breakpoint transitions:
       - Mobile: 1 column, widgets stacked.
       - Desktop: 3-4 columns with specific spans for Now/Today/Context.
    2. Wrap widgets in Framer Motion components to add staggered entry animations.
    3. Ensure that the layout doesn't shift unexpectedly when widgets are in loading/empty states.
  </action>
  <verify>
    Check responsive classes in IndexPage.tsx (e.g., md:col-span-2) and Framer Motion variant usage.
  </verify>
  <done>Dashboard is responsive and has fluid entry animations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>3-Zone Dashboard Layout with animated widgets</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit the dashboard (usually /dashboard or root if authenticated).
    3. Verify three distinct zones (Now, Today, Context) are visible and visually balanced.
    4. Verify widgets have entrance animations.
    5. Test responsiveness by resizing the browser window (1 col -> 3 cols).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual verification of layout balance and responsiveness.
</verification>

<success_criteria>
- Layout matches the 3-zone PRD requirement.
- Grid is responsive across mobile and desktop.
- Animations are subtle and professional.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-infrastructure-dashboard-layout/02-02-SUMMARY.md`
</output>
