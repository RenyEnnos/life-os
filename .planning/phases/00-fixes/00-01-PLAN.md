# Plan: 00-01 Database Schema & Seeds

**Phase:** 00 - Fixes
**Plan:** 00-01
**Status:** Ready to execute

## Goal
Ensure the database schema is consistent and contains essential seed data for new users.

## Context
- The `transactions` table uses `transaction_date` while code sometimes expects `date`. (Decision: Keep code using `transaction_date`).
- New users lack default financial categories, causing UI issues when logging expenses.

## Task Breakdown
1. **Financial Categories Seed**: Create a SQL script to insert default income/expense categories.
   - Files: `supabase/migrations/20240326000001_seed_finance_categories.sql`
2. **Auto-Seed on Register**: Update the registration flow to automatically create these categories for new users.
   - Files: `api/routes/auth.ts` (Check if there's a post-registration hook or logic).
3. **Schema Documentation**: Create a markdown file describing the current core tables (Habits, Tasks, Transactions, Budgets).
   - Files: `docs/database-schema-v2.md`

## Verification
- [ ] New user registers and immediately sees default categories in the Finance page.
- [ ] Database schema document reflects real table structures.
- [ ] SQL migration runs successfully without errors.

## Agent Assignments
- **Database**: `database-architect`
- **Auth Flow**: `backend-specialist`
