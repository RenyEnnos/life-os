# Plan: 00-03 Onboarding & E2E Tests

**Phase:** 00 - Fixes
**Plan:** 00-03
**Status:** Ready to execute

## Goal
Improve user retention via onboarding improvements and ensure stability with automated tests.

## Context
- Onboarding is currently hard to skip and doesn't persist state efficiently.
- Essential E2E flows (Auth, Finances) are missing or incomplete.

## Task Breakdown
1. **Onboarding UX**: Add a "Skip" button, persist completion in `localStorage`, and ensure the flow is only shown once per user.
   - Files: `src/features/onboarding/components/OnboardingManager.tsx`
2. **Auth E2E Tests**: Implement Playwright tests for Login, Register, and Logout flows.
   - Files: `tests/e2e/auth.spec.ts`
3. **Finances E2E Tests**: Basic sanity check for the Finance page (Ensure it loads with categories).
   - Files: `tests/e2e/finances.spec.ts`

## Verification
- [ ] User completes onboarding -> Refresh -> Onboarding doesn't show up again.
- [ ] Playwright `auth.spec.ts` passes for register/login.
- [ ] Playwright `finances.spec.ts` passes (Dashboard loads with categories).

## Agent Assignments
- **Onboarding UI**: `frontend-specialist`
- **Tests**: `test-engineer`
