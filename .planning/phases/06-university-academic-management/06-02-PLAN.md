# Plan: 06-02 University UI Integration

**Phase:** 06 - University & Academic Management
**Plan:** 06-02
**Status:** Ready to execute

## Goal
Replace the mock data in the main university components with real data from the `useUniversity` hook.

## Context
- `CourseGrid.tsx`, `AssignmentList.tsx`, and `ScheduleWidget.tsx` are using hardcoded mock constants.
- The `useUniversity` hook is already implemented and should be used to provide real data.

## Task Breakdown
1. **Refactor CourseGrid**: Replace `COURSES` mock with `useUniversity().courses`.
   - Files: `src/features/university/components/CourseGrid.tsx`
2. **Refactor AssignmentList**: Replace `ASSIGNMENTS` mock with `useUniversity().assignments`.
   - Files: `src/features/university/components/AssignmentList.tsx`
3. **Refactor ScheduleWidget**: Use the `courses` and `assignments` data to populate the schedule.
   - Files: `src/features/university/components/ScheduleWidget.tsx`
4. **Interactive Updates**: Ensure adding/deleting assignments or courses updates the UI immediately (optimistic updates if possible).
   - Files: `src/features/university/hooks/useUniversity.ts`

## Verification
- [ ] Courses added by the user are visible in the grid.
- [ ] Assignments are correctly listed and show their status.
- [ ] Switching between courses filters the assignment list.

## Agent Assignments
- **Frontend**: `frontend-specialist`
- **Hooks/Logic**: `backend-specialist`
