# Plan: 06-01 University Database & CRUD

**Phase:** 06 - University & Academic Management
**Plan:** 06-01
**Status:** Ready to execute

## Goal
Establish the database foundations for the university feature and ensure basic CRUD operations for courses and assignments are functional.

## Context
- The backend services and routes for university already exist but they point to non-existent tables.
- Frontend components are using mock data.

## Task Breakdown
1. **DB Migration**: Create `supabase/migrations/013_add_university_tables.sql` defining `courses` and `assignments` tables.
   - Files: `supabase/migrations/013_add_university_tables.sql`
2. **RLS Policies**: Implement Row Level Security for university tables in the migration.
3. **Frontend Hook Review**: Verify `useUniversity.ts` and ensure it handles API errors correctly.
   - Files: `src/features/university/hooks/useUniversity.ts`
4. **Course Management UI**: Implement `CreateCourseModal.tsx` and ensure it saves data to the backend.
   - Files: `src/features/university/components/CreateCourseModal.tsx`

## Verification
- [ ] SQL migration runs without errors.
- [ ] A new course can be created and persists in the database.
- [ ] Assignments can be listed (even if empty) for a specific user.

## Agent Assignments
- **Database**: `database-architect`
- **Backend/Hooks**: `backend-specialist`
- **Frontend**: `frontend-specialist`
