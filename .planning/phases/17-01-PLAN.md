---
phase: 17-eradicate-mocks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/university/components/CourseCard.tsx
  - src/features/university/components/CourseGrid.tsx
  - src/features/university/components/WhatIfSimulator.tsx
  - src/features/university/components/UniversityWidget.tsx
  - api/services/contextGateway.ts
autonomous: true
requirements: [MOCKS-01]
must_haves:
  truths:
    - "Course Card shows actual calculated progress instead of a hardcoded 75%"
    - "What-If Simulator uses real assignment weights to calculate required grades"
    - "Dashboard Context does not show hardcoded mock data (like Bitcoin at $65,000) when APIs fail"
  artifacts:
    - path: "src/features/university/components/CourseCard.tsx"
      provides: "Course card with dynamic progress prop"
    - path: "src/features/university/components/WhatIfSimulator.tsx"
      provides: "Simulator with real weight calculations"
    - path: "api/services/contextGateway.ts"
      provides: "Real API fallback handling without mocks"
  key_links:
    - from: "src/features/university/components/CourseGrid.tsx"
      to: "src/features/university/components/CourseCard.tsx"
      via: "Passes calculated progress prop"
    - from: "src/features/university/components/UniversityWidget.tsx"
      to: "src/features/university/components/WhatIfSimulator.tsx"
      via: "Passes assignments prop"
---

<objective>
Eradicate hardcoded mock data across University features and API Gateway.

Purpose: Ensure the application displays real, calculated data based on user input, and degrades gracefully when external APIs fail, instead of showing confusing static data.
Output: Updated UI components that compute real progress and weights, and an API service that returns empty states on failure.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
@/home/pedro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dynamic Course Progress</name>
  <files>src/features/university/components/CourseCard.tsx, src/features/university/components/CourseGrid.tsx</files>
  <action>
    Update `CourseCard.tsx` to accept a `progress: number` prop in its interface and remove the hardcoded `const progress = 75;`.
    Update `CourseGrid.tsx` to calculate the progress for each course. Use the `useGradeCalculation(assignments)` hook to get the `calculateProgress` function.
  </action>
  <verify>grep -q "progress" src/features/university/components/CourseCard.tsx</verify>
  <done>Course cards display dynamically calculated progress percentages.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Real What-If Simulator Weights</name>
  <files>src/features/university/components/WhatIfSimulator.tsx, src/features/university/components/Sidebar.tsx</files>
  <action>
    Refactor `WhatIfSimulator.tsx` to accept an `assignments: Assignment[]` prop in addition to `courses`.
    Remove the hardcoded `const remainingWeight = 0.4;` and `currentWeight = 0.6;`.
    Dynamically calculate total weight, completed weight (from graded assignments), and remaining weight for the selected course.
    Update the parent component rendering `WhatIfSimulator` (likely `Sidebar.tsx` or `UniversityWidget.tsx`) to pass the real `assignments` array fetched from `useUniversity()` down to the simulator.
  </action>
  <verify>grep -q "assignments" src/features/university/components/WhatIfSimulator.tsx</verify>
  <done>What-If simulator computes required grades using actual assignment weights.</done>
</task>

<task type="auto">
  <name>Task 3: Eradicate API Gateway Mocks</name>
  <files>api/services/contextGateway.ts</files>
  <action>
    Remove the `MOCKS` object completely from `api/services/contextGateway.ts`.
    Update error handling blocks that previously returned mock data to instead return graceful fallbacks:
    - Crypto API errors should return `null` or `{}` instead of `MOCKS.crypto`.
    - Weather API errors should return `null` instead of `MOCKS.weather`.
    - News API errors should return `[]` (empty array) instead of `MOCKS.news`.
  </action>
  <verify>! grep -q "MOCKS" api/services/contextGateway.ts</verify>
  <done>Context gateway handles API failures by returning empty states, not mock data.</done>
</task>

</tasks>

<verification>
1. Run application and view the University dashboard.
2. Confirm course progress displays dynamically based on actual assignments.
3. Confirm What-If simulator uses real assignment weights and computes realistically.
4. Simulate API failure (or check gateway responses) and confirm Context dashboard degrades gracefully without showing hardcoded Bitcoin data.
</verification>

<success_criteria>
All hardcoded mock data (`75%` progress, `0.4` remaining weight, `MOCKS` object) are completely removed from the specified files and replaced with real computations or graceful empty states.
</success_criteria>

<output>
After completion, create `.planning/phases/17-eradicate-mocks/17-01-SUMMARY.md`
</output>