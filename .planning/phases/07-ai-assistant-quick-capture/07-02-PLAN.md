# Plan: 07-02 Universal Quick Capture

**Phase:** 07 - AI Assistant & Quick Capture
**Plan:** 07-02
**Status:** Ready to execute

## Goal
Promote `QuickCapture` to a global utility and enhance its NLP capabilities to handle Tasks, Habits, and Expenses.

## Context
- `QuickCapture` currently only exists inside the Tasks feature.
- Users should be able to press a shortcut (e.g., `Cmd+K` or a header button) to capture anything.

## Task Breakdown
1. **Universal Parser**: Update the backend `parseTask` logic (if needed) or create a broader `parseEntity` to distinguish between habits, tasks, and transactions.
   - Files: `api/services/aiService.ts`
2. **Global Header Capture**: Add a permanent Quick Capture bar or trigger in the main application header.
   - Files: `src/shared/components/Header.tsx` (or similar)
3. **NLP Improvements**: Refine the system prompt for NLP parsing to ensure higher accuracy for Brazilian Portuguese.
   - Files: `api/services/aiService.ts`

## Verification
- [ ] User can trigger Quick Capture from any page.
- [ ] Typing "Gastei 50 reais com pizza" correctly creates a transaction.
- [ ] Typing "Correr 5km amanhã" correctly creates a task.

## Agent Assignments
- **NLP Logic**: `backend-specialist`
- **Global UI**: `frontend-specialist`
