# Plan: 07-03 Synapse AI Insights

**Phase:** 07 - AI Assistant & Quick Capture
**Plan:** 07-03
**Status:** Ready to execute

## Goal
Integrate the `synapseSuggestionsService` into the main dashboard to provide real-time, context-aware AI insights to the user.

## Context
- The logic for generating suggestions based on tasks, habits, and health metrics is already in the backend.
- The user needs a visual place to see and act on these suggestions.

## Task Breakdown
1. **SynapseWidget Component**: Create a premium bento card that fetches and displays the top 3 suggestions from `/api/synapse/suggestions`.
   - Files: `src/features/dashboard/widgets/SynapseWidget.tsx`
2. **Action Handling**: Implement the logic to execute the `action_label` (e.g., open a task, log a habit) when a suggestion is clicked.
   - Files: `src/features/dashboard/widgets/SynapseWidget.tsx`
3. **Dashboard Placement**: Replace the placeholder "AI Recommendations" card in the `IndexPage` with the real `SynapseWidget`.
   - Files: `src/features/dashboard/pages/IndexPage.tsx`

## Verification
- [ ] Suggestions are displayed on the dashboard after a short delay.
- [ ] Suggestions reflect the current state of the user's life (e.g., "Drink water" if hydration is low).
- [ ] Clicking a suggestion performs the expected action.

## Agent Assignments
- **UI/UX**: `frontend-design`, `frontend-specialist`
- **Integration**: `orchestrator`
