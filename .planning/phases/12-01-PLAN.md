---
phase: 12-premium-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tailwind.config.ts
  - src/features/finances/components/FinanceSummaryCards.tsx
  - src/features/finances/components/FinanceCharts.tsx
  - src/features/habits/components/HabitContributionGraph.tsx
autonomous: true
requirements: [GEMS-01]
---

<objective>
Elevate the visual quality of data visualization across the application by installing Tremor v3 and refactoring core dashboard components (Finance and Habits) to use Tremor's premium UI components.

Purpose: Achieve a consistent, professional SaaS aesthetic.
Output: Modernized finance and habit dashboards.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-research.md
@src/features/finances/components/FinanceSummaryCards.tsx
@src/features/finances/components/FinanceCharts.tsx
@src/features/habits/components/HabitContributionGraph.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tremor and Configure Tailwind</name>
  <files>package.json, tailwind.config.ts</files>
  <action>
    1. Install the required Tremor v3 dependencies:
       `npm install @tremor/react @headlessui/react @tailwindcss/forms @remixicon/react`
    2. Extend `tailwind.config.ts` to support Tremor:
       - Add the content path: `"./node_modules/@tremor/react/**/*.{js,ts,jsx,tsx}"`
       - Add the `@tailwindcss/forms` plugin.
       - Configure the Tremor-specific color palette (tremor-brand, tremor-background, etc.) and shadow/border-radius settings as per Tremor v3 documentation.
       - Ensure the configuration supports both light and dark modes.
  </action>
  <verify>npm list @tremor/react && npx tailwindcss -i ./src/index.css -o ./dist/output.css --dry-run</verify>
  <done>Tremor dependencies are installed and Tailwind is configured to process Tremor classes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Finance Dashboard Components</name>
  <files>src/features/finances/components/FinanceSummaryCards.tsx, src/features/finances/components/FinanceCharts.tsx</files>
  <action>
    1. Refactor `FinanceSummaryCards.tsx`:
       - Replace custom card and metric implementations with Tremor's `Card`, `Metric`, `Text`, and `BadgeDelta` components.
       - Use `BadgeDelta` to visually indicate positive/negative financial trends.
    2. Refactor `FinanceCharts.tsx`:
       - Replace existing Recharts implementation with Tremor's `AreaChart` (for expense trends) and `DonutChart` (for category distribution).
       - Ensure data mapping aligns with Tremor's API while maintaining existing logic for filtering and periods.
  </action>
  <verify>Visual inspection of the Finance dashboard to ensure charts and cards render with the new Tremor aesthetic.</verify>
  <done>Finance summaries and charts are fully migrated to Tremor v3.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Habit Contribution Graph</name>
  <files>src/features/habits/components/HabitContributionGraph.tsx</files>
  <action>
    Refactor the `HabitContributionGraph.tsx` to use Tremor's `Tracker` component for visualizing daily completion consistency.
    Map the habit completion data (last 30 days) to the `Tracker` data format, using Tremor's color system to indicate intensity/completion status.
  </action>
  <verify>Check the Habit dashboard to confirm the contribution graph displays correct historical data using the Tracker component.</verify>
  <done>The habit heatmap is replaced with a premium Tremor Tracker component.</done>
</task>

</tasks>

<verification>
- **Visual Audit**: Manually verify the dashboards for consistent spacing, colors, and typography.
- **Linting**: Run `npm run lint` to catch any prop-type or import errors introduced during refactoring.
- **PWA Manifest**: Ensure no regressions in the PWA configuration or manifest.
</verification>

<success_criteria>
- Tremor v3 dependencies and Tailwind configuration are correctly set up.
- Finance summary cards use Metric and BadgeDelta components.
- Finance charts use AreaChart and DonutChart from Tremor.
- Habit contribution graph uses the Tracker component.
- App maintains full responsiveness and dark mode support.
</success_criteria>

<output>
After completion, create `.planning/phases/12-premium-dashboards/12-01-SUMMARY.md`
</output>
