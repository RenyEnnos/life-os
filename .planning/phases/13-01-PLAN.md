---
phase: 13-robust-forms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/shared/ui/Input.tsx, src/shared/schemas/auth.ts, src/shared/schemas/tasks.ts, src/shared/schemas/finances.ts, src/features/auth/components/LoginPage.tsx, src/features/auth/components/RegisterPage.tsx, src/features/tasks/components/CreateTaskForm.tsx]
autonomous: true
requirements: [GEMS-02]
---

<objective>
Standardize form management and validation across the application using React Hook Form (RHF) and Zod. This plan covers the foundation (installation and schemas) and the refactoring of core authentication and task forms.
</objective>

<execution_context>
@/home/pedro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/shared/ui/Input.tsx
@src/features/auth/components/LoginPage.tsx
@src/features/auth/components/RegisterPage.tsx
@src/features/tasks/components/CreateTaskForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies and Define Schemas</name>
  <files>package.json, src/shared/schemas/auth.ts, src/shared/schemas/tasks.ts, src/shared/schemas/finances.ts</files>
  <action>
    1. Install `react-hook-form`, `zod`, and `@hookform/resolvers`.
    2. Create `src/shared/schemas/auth.ts` with schemas for Login (email, password) and Register (email, password, confirmPassword).
    3. Create `src/shared/schemas/tasks.ts` with schemas for Task creation (title, description, due_date, tags).
    4. Create `src/shared/schemas/finances.ts` with schemas for Transaction (amount, category, description, date).
  </action>
  <verify>npm list react-hook-form zod @hookform/resolvers</verify>
  <done>Dependencies installed and centralized schemas created.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor UI Components and Auth Forms</name>
  <files>src/shared/ui/Input.tsx, src/features/auth/components/LoginPage.tsx, src/features/auth/components/RegisterPage.tsx</files>
  <action>
    1. Update `Input.tsx` to ensure it properly handles the `error` prop and displays error messages from Zod.
    2. Refactor `LoginPage.tsx` to use `useForm` from RHF with the `zodResolver`. Replace manual state management (`email`, `password`, `error`) with RHF's `register` and `formState`.
    3. Refactor `RegisterPage.tsx` similarly to use RHF + Zod.
  </action>
  <verify>Manual check of login/register validation triggers error messages.</verify>
  <done>Auth forms refactored to use RHF + Zod validation.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Task Forms and Verification</name>
  <files>src/features/tasks/components/CreateTaskForm.tsx</files>
  <action>
    1. Refactor `CreateTaskForm.tsx` to use RHF + Zod for task creation validation.
    2. Implement error display for required fields (e.g., Title).
    3. Verify that form submission still calls the `onSubmit` prop correctly with validated data.
    4. Run a performance check to ensure RHF minimizes re-renders compared to the previous manual state approach.
  </action>
  <verify>Create task with empty title fails validation; with valid data calls onSubmit.</verify>
  <done>Task forms refactored and overall validation verified.</done>
</task>

</tasks>

<success_criteria>
1. All target forms (Login, Register, CreateTask) use React Hook Form for state.
2. Validation is strictly enforced by Zod schemas.
3. UI components display validation errors clearly.
4. Performance remains high with optimized re-renders.
</success_criteria>
