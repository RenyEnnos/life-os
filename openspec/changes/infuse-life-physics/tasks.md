# Tasks: Life & Physics

- [x] **Atmosphere Injection** <!-- id: atmosphere-applayout -->
    - [x] Add `.bg-noise` to `AppLayout` wrapper.
    - [x] Refine `.vignette-radial` and add scrollbar styling.
    - [x] Verify `mix-blend-mode: overlay` opacity on dark mode.
- [x] **Bento Physics** <!-- id: physics-bento -->
    - [x] Refactor `BentoCard.tsx` to use `scale: 1.01` hover.
    - [x] Standardize borders to `border-white/5`.
    - [x] Implement `bg-white/[0.02]` surface highlight.
- [x] **Modal Fluidity** <!-- id: physics-modal -->
    - [x] Refactor `Modal.tsx` to use `Framer Motion` + `AnimatePresence`.
    - [x] Implement spring physics (`stiffness: 300, damping: 30`).
    - [x] Add `backdrop-blur-md` to overlay.
- [x] **Mobile & Polish** <!-- id: mobile-safe -->
    - [x] Update `AppLayout` padding for `env(safe-area-inset-bottom)`.
    - [x] Verify `Deep Focus` transitions.
