# Tasks: Deep Glass Refinements Implementation

1.  **Refine Cinematic Flow**
    - [ ] Update `src/app/layout/AppLayout.tsx` to remove `y` property from `pageVariants`.
    - [ ] Verify `ScrollToTop` component behavior is maintained.

2.  ** Implement Deep Inputs**
    - [ ] Refactor `src/shared/ui/Input.tsx` with the new "Deep Glass" styling (inner shadow, glow).
    - [ ] Ensure `TextArea` is preserved (even if style is basic for now, or apply similar styles if trivial).
    - [ ] Verify focus states and "glow" animation.
