# Tasks: Deep Glass Refinements Implementation

1.  **Refine Cinematic Flow**
    - [x] Update `src/app/layout/AppLayout.tsx` to remove `y` property from `pageVariants`.
    - [x] Verify `ScrollToTop` component behavior is maintained.

2.  ** Implement Deep Inputs**
    - [x] Refactor `src/shared/ui/Input.tsx` with the new "Deep Glass" styling (inner shadow, glow).
    - [x] Ensure `TextArea` is preserved (even if style is basic for now, or apply similar styles if trivial).
    - [x] Verify focus states and "glow" animation.
